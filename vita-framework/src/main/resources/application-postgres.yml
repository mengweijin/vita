spring:
  liquibase:
    contexts: prod
    change-log: classpath:/db/db.changelog-postgres.xml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://locahost:5432/postgres
    username:
    password:
