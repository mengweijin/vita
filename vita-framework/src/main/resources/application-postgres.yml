spring:
  liquibase:
    contexts: prod
    change-log: classpath:/db/db.changelog-postgres.yaml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://locahost:5432/postgres
    username:
    password:
