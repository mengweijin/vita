spring:
  liquibase:
    contexts: prod
    change-log: classpath:/db/db.changelog-oracle.xml
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@//localhost:1521/ORCLCDB
    username:
    password:
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      password:
decorator:
  datasource:
    enabled: true
