spring:
  liquibase:
    contexts: prod
    change-log: classpath:/db/db.changelog-postgresql.xml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://locahost:5432/postgres
    username:
    password:
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      password:
decorator:
  datasource:
    enabled: true
