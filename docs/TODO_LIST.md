# Todo List

- 后端
  - mybatis plus 数据权限插件。
  - aop 实现审计日志功能。
  - 集成 MapStruct，统一增加 vo, bo, do。
  - @Configuration 改为 @AutoConfiguration。
  - LoginUserVO 用户图像不应缓存到会话中。
  - Ip2Region 更新。
  - 调度任务启用/停用状态需要控制实际 cron 的执行。当前未控制，默认应用启动拉起启用的调度任务。
  - 登出后，SSE 后端应该同步移除。
- 前端
  - 代码生成器。
  - 系统主题颜色，页脚隐藏开关。
  - 页面权限
  - 按钮权限。
  - cropperjs 图像裁剪。
